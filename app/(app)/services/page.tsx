import { SectionPlaceholder } from "../components/section-placeholder";

export default function ServicesPage() {
  return (
    <SectionPlaceholder
      description="Manage your bookable services, durations, and pricing from a single catalog."
      hint="No services configured yet. Add one to start accepting bookings."
      title="Services"
    />
  );
}
