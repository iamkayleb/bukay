import { SectionEmptyState } from "../_components/section-empty-state";

export default function SettingsPage() {
  return (
    <SectionEmptyState
      eyebrow="Settings"
      title="Settings"
      description="Workspace preferences, team access, and booking policies will appear here when configured."
    />
  );
}
