# public/

Static assets served at the root of the site by Next.js.

Files placed here are exposed at `/<filename>`. For example, `public/logo.svg`
would be reachable at `/logo.svg`.

See: https://nextjs.org/docs/app/building-your-application/optimizing/static-assets
